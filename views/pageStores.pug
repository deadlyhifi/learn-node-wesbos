extends layout

include mixins/storeForm
include mixins/storeCard

block content
    .inner
        h2 🎸 #{title}
        .stores
            each store in stores
                +storeCard(store)
